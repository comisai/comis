---
phase: 07-documentation
plan: 06
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - website/src/components/Security.astro
  - website/src/components/Features.astro
  - website/src/components/Comparison.astro
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - "Security.astro threats array has a credential broker entry with threat/answer/detail"
    - "Features.astro features array has a credential broker feature cell"
    - "Comparison.astro rows array has a credentialed exec row"
    - "All wording uses pre-P4 safe framing — no 'non-bypassable' as guarantee"
    - "Provider count = 2 in any Astro claims"
  artifacts:
    - path: "website/src/components/Security.astro"
      provides: "Broker threat row in threats array"
      contains: "Credential broker"
    - path: "website/src/components/Features.astro"
      provides: "Broker feature cell in features array"
      contains: "Credential broker"
    - path: "website/src/components/Comparison.astro"
      provides: "Credentialed exec comparison row"
      contains: "Credentialed exec"
  key_links:
    - from: "website/src/components/Security.astro"
      to: "Credential broker"
      via: "threats array entry"
      pattern: "Credential broker"
---

<objective>
Update three Astro homepage components to surface the credential broker differentiator on the homepage.

Purpose: Website visitors see the broker in the Security threats matrix, the Features grid, and the Comparison table — all with publish-gate-safe framing. The Astro build must remain clean.

Output: Three updated Astro component files with broker additions to their data arrays.
</objective>

<execution_context>
@/Users/mosheanconina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosheanconina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-documentation/07-RESEARCH.md
@.planning/phases/07-documentation/07-01-SUMMARY.md

<interfaces>
<!-- Astro component data patterns — from source inspection -->

Security.astro threats array item shape:
{ threat: string, desc: string, answer: string, detail: string }

Features.astro features array item shape:
{ icon: string, title: string, desc: string }

Comparison.astro rows array item shape:
{ label: string, them: string, us: string, win: boolean }

Wording constraints (PUBLISH GATE + ACCURACY):
- Do NOT use "non-bypassable" or "kernel-locked egress" as a guarantee
- Use: "broker-only network mode" / "designed-to-be-only" / "R1 validation in progress" framing
- Provider count: exactly 2 (never "N providers", never >2)
- Comparison differentiator wording from RESEARCH.md:
  label: "Credentialed exec — keys never in sandbox"
  them: "Relies on agent volunteering to use HTTPS_PROXY; bypass is possible"
  us: "Broker-only network namespace (Linux, R1 in progress); key never enters the sandbox; fails closed without the key"

Security.astro broker entry (from RESEARCH.md):
  threat: "Credential exposure via agentic CLIs"
  answer: "Credential broker."
  detail: "Key kept out of the sandbox — injected at the network boundary. `cat`/`env`/`/proc` show nothing. Broker fails closed."
  desc: explanation of the problem being solved

Features.astro broker entry:
  icon: "key" (or closest available icon — check the other features icons to match: "context", "automate", "tools", "media", "models", "memory")
  title: "Credential broker"
  desc: "Drive API-key CLIs — Claude Code included — with the key kept out of the sandbox. Injected at the network boundary from the daemon's encrypted store; the CLI authenticates, your key stays in the daemon. Anthropic and Finnhub today."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Security.astro</name>
  <files>website/src/components/Security.astro</files>
  <action>
Read the file. Find the threats array. The current last entry is about "Unchecked tool access" (based on the first 40 lines already read).

Add a new entry to the threats array. Position it after the existing "Credential exposure" (API keys in logs) entry — since it's a stronger/deeper form of the same threat category. Use this exact object:

```js
{
  threat: "Credential exposure via agentic CLIs",
  desc: "An agent running shell commands can read any secret in its environment — `cat ~/.claude/.credentials.json`, `env`, `/proc/self/environ`. Handing a key to the agent process means it can steal and exfiltrate it.",
  answer: "Credential broker.",
  detail: "The real key never enters the sandbox. It stays in the daemon's encrypted store; the CLI runs with a placeholder. The broker injects the real credential at the TLS network boundary — `cat`/`env`/`/proc` inside the sandbox show nothing. Broker fails closed: no secret → 502, request never forwarded.",
},
```

Do NOT change any other array entries or any component template code. Only add the new object to the threats array.
  </action>
  <verify>
    <automated>grep -c "Credential exposure via agentic CLIs" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/website/src/components/Security.astro</automated>
  </verify>
  <done>Security.astro threats array contains the credential-broker entry with the exact threat/desc/answer/detail fields.</done>
</task>

<task type="auto">
  <name>Task 2: Update Features.astro and Comparison.astro</name>
  <files>website/src/components/Features.astro, website/src/components/Comparison.astro</files>
  <action>
Read both files fully (already have Features.astro from context — confirm the icon names used: "context", "automate", "tools", "media", "models", "memory").

**Features.astro:**
Add a new entry to the features array. Append as the last item (or insert after "Encrypted at rest" equivalent if one exists):

```js
{ icon: "key", title: "Credential broker", desc: "Drive API-key CLIs — Claude Code included — with the key kept out of the sandbox. Injected at the network boundary from the daemon's encrypted store; the CLI authenticates while your key stays in the daemon. Anthropic and Finnhub today, more on the catalog roadmap." },
```

Note: if "key" is not a valid icon name in the Astro component, use a similar one from the existing list. The component renders `{f.icon}` — check how icons are resolved. If icons are just strings displayed as text labels (not icon component imports), "key" is safe. If they're resolved from an icon set, check the pattern used and pick the closest match.

**Comparison.astro:**
Add a new entry to the rows array. Insert after the "Secrets" row (or "OS-level exec sandbox" row, whichever is more thematically appropriate — after "Secrets" is best since this is about credential security):

```js
{ label: "Credentialed exec — keys never in sandbox", them: "Relies on agent volunteering to use HTTPS_PROXY; bypass is possible", us: "Broker-only network namespace (Linux, R1 in progress); key never enters the sandbox; fails closed without the key", win: true },
```

Do NOT change any existing rows or template code in either file. Only add the new array entries.
  </action>
  <verify>
    <automated>grep -c "Credential broker" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/website/src/components/Features.astro && grep -c "Credentialed exec" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/website/src/components/Comparison.astro</automated>
  </verify>
  <done>Features.astro features array has a "Credential broker" entry. Comparison.astro rows array has a "Credentialed exec" entry with win: true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| website components → public visitors | Website claims are read by potential users evaluating security posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Information Disclosure | Security.astro / Comparison.astro network enforcement claim | mitigate | "R1 in progress" qualifier in Comparison row; "broker fails closed" factual; no "non-bypassable" guarantee language |
| T-07-16 | Information Disclosure | Features.astro provider count | mitigate | "Anthropic and Finnhub today" — exactly 2, plus "more on catalog roadmap" framing |
</threat_model>

<verification>
After both tasks:
1. `grep -c "Credential exposure via agentic CLIs" website/src/components/Security.astro` → 1
2. `grep -c "Credential broker" website/src/components/Features.astro` → ≥ 1
3. `grep -c "Credentialed exec" website/src/components/Comparison.astro` → ≥ 1
4. `grep -v "^#\|//" website/src/components/Security.astro | grep -c "non-bypassable\|kernel-locked egress"` → 0
</verification>

<success_criteria>
- Security.astro: broker threat entry with threat/desc/answer/detail fields
- Features.astro: broker feature cell with "Anthropic and Finnhub today" (count = 2)
- Comparison.astro: credentialed exec row with "R1 in progress" qualifier and "fails closed" claim
- No "non-bypassable" or "kernel-locked egress" guarantee in any Astro component addition
- Astro build is validated in plan 07-07 (this plan focuses on component data; build check in 07-07)
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation/07-06-SUMMARY.md`
</output>
