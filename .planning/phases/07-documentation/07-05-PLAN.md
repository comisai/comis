---
phase: 07-documentation
plan: 05
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - "README.md Why Comis section has a broker benefit bullet using the pre-P4 safe variant"
    - "README.md How Comis is different table has a credentialed exec row"
    - "README.md Features grid has a credential broker cell"
    - "README.md FAQ has a broker details block"
    - "Provider count is exactly 2 (Anthropic and Finnhub today) — not a larger number"
    - "Publish-gate marker present in the README where strong containment claim would appear"
    - "No invented metrics or latency numbers anywhere in README additions"
    - "executor.broker config example in README is byte-identical to the canonical block from 07-01"
  artifacts:
    - path: "README.md"
      provides: "Four broker additions: Why Comis bullet, comparison row, features grid cell, FAQ entry"
      contains: "credential-broker"
  key_links:
    - from: "README.md"
      to: "https://docs.comis.ai/security/credential-broker"
      via: "deep-dive link"
      pattern: "docs.comis.ai/security/credential-broker"
---

<objective>
Insert the four credential broker benefit blocks into README.md, using the pre-P4 safe variant with all placeholders resolved to verified values.

Purpose: GitHub README is the primary first impression. The four blocks (Why Comis bullet, comparison table row, features cell, FAQ entry) surface the broker differentiator to users before they visit the docs.

Output: README.md updated with 4 broker additions. No existing README content changed.
</objective>

<execution_context>
@/Users/mosheanconina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosheanconina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/design/credential-broker-README-benefit-draft.md
@.planning/phases/07-documentation/07-RESEARCH.md
@.planning/phases/07-documentation/07-01-SUMMARY.md

<interfaces>
<!-- Resolved placeholders — use EXACTLY these values -->

‹N› → 2 (phrase: "Anthropic and Finnhub today")
‹deep-dive-url› → https://docs.comis.ai/security/credential-broker
‹forced-egress-mechanism› → DO NOT use as a guarantee; use: "with its network namespace isolated (`--unshare-net`)"
OAuth status: still deferred — keep "API-key / bearer / query-param auth today" sentence
All ‹metric› / latency numbers → DELETE the clause entirely (no unverified numbers)

PUBLISH GATE: use the pre-P4 safe variant from the benefit draft, NOT Block 1.
Add <!-- publish gate: flip to Block 1 post-R1 --> HTML comment where the strong version would go.

Pre-P4 safe variant (from benefit draft):
> 🔑 **Credential broker (in progress — network enforcement pending Linux validation).** Comis drives
> API-key CLIs (Claude Code included) with the key kept out of the sandbox — injected at the
> network boundary from the daemon's encrypted store, so it's never a readable file, env var, or
> `/proc` entry inside the sandbox. On Linux, the credentialed sandbox runs with its network
> namespace isolated (`--unshare-net`), with the broker unix socket as the only bind-mounted
> network path — making the broker the designed-to-be-only destination. Validation of this
> kernel-enforcement on the Linux production host class is in progress. [Deep dive →](https://docs.comis.ai/security/credential-broker)

Block 2 comparison row (qualify language for current state):
"Credentialed exec, keys never in sandbox" | Comis: "Broker-only network mode (Linux, R1 in progress); key never enters sandbox; fails closed without key"

Block 3 features cell (use publish-gate-safe wording):
🔑 **Credential broker**
Drive API-key CLIs — Claude Code included — with the key kept out of the sandbox. Injected at the network boundary from the daemon's encrypted store; audited per request.

Block 4 FAQ entry — use pre-P4 safe wording for containment claim:
Replace ‹forced-egress-mechanism› with: "network namespace isolation (`--unshare-net`) — the broker unix socket is the only bind-mounted network path (validation on the Linux production host class is in progress)"
Replace ‹N› with: "2"
Replace "Anthropic, GitHub PATs, Finnhub, and ‹N› others" with: "API-key / bearer / query-param auth (Anthropic and Finnhub today, more on the catalog roadmap)"

README insertion points (from analysis):
- "Why Comis" → line ~78, add the pre-P4 variant bullet AFTER the defense-in-depth bullet
- "How Comis is different" table → after the "OS-level exec sandbox" row (~line 108)
- "Features" grid → add a row/cell after the encrypted-at-rest cell (end of the 3-column table)
- "FAQ" → add the <details> block after the existing secrets FAQ entry

The executor.broker config example in the FAQ should be BYTE-IDENTICAL to:
```yaml
executor:
  broker:
    bindings:
      - preset: anthropic
        secretRef: ANTHROPIC_EXECUTOR_KEY
      - preset: finnhub
        secretRef: FINNHUB_API_KEY
```
(The condensed form — just the two presets — is appropriate for the FAQ. The full 4-option example is in the docs.)

NOTE: The FAQ block should also note that executor.broker daemon wiring is in progress (match the tone of the docs Warning — one brief parenthetical is enough for the README audience):
"(The executor.broker config path is the planned interface; daemon integration is in progress.)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert broker additions into README.md</name>
  <files>README.md</files>
  <action>
Read README.md in full. Identify the exact insertion points for each of the four blocks. Make all four additions in a single edit.

**Insertion 1 — "Why Comis" section (after the defense-in-depth bullet, before the supply-chain bullet):**

Add immediately after the `**🛡️ Defense in depth...` bullet (after its trailing `[Deep dive →]` link and blank line):

```
<!-- publish gate: flip to Block 1 post-R1 -->
**🔑 Credential broker (in progress — network enforcement pending Linux validation).** Comis drives API-key CLIs (Claude Code included) with the key kept out of the sandbox — injected at the network boundary from the daemon's encrypted store, so it's never a readable file, env var, or `/proc` entry inside the sandbox. On Linux, the credentialed sandbox runs with its network namespace isolated (`--unshare-net`), with the broker unix socket as the only bind-mounted network path — making the broker the designed-to-be-only destination. Validation of this kernel-enforcement on the Linux production host class is in progress. [Deep dive →](https://docs.comis.ai/security/credential-broker)
```

**Insertion 2 — "How Comis is different" comparison table (after "OS-level exec sandbox" row):**

After the row `| OS-level exec sandbox (kernel-enforced) | ✅ | ❌ | ❌ | ❌ | ❌ |`, add:

```
| Credentialed exec — keys never in sandbox (broker-only egress, Linux) | ✅ | ❌ | ❌ | ❌ | ❌ |
```

**Insertion 3 — "Features" grid (add a new row after the existing 3-column table's last row):**

The current features table has two rows of 3 cells. Add a new row with the broker cell. Append after the last `|` row:

```
| 🔑 **Credential broker** | | |
| Drive API-key CLIs — Claude Code included — with the key kept out of the sandbox. Injected at the network boundary from the daemon's encrypted store; audited per request. (Anthropic and Finnhub today.) [Deep dive →](https://docs.comis.ai/security/credential-broker) | | |
```

Note: if the table formatting doesn't allow a partial row, place the broker cell as the first cell of a new 3-column row with empty remaining cells, consistent with how the existing features table handles partial rows. Match the existing whitespace/alignment style exactly.

**Insertion 4 — "FAQ" section (add after the existing "How does Comis handle secrets?" block):**

After the closing `</details>` of the secrets FAQ entry, add:

```md
<details>
<summary><strong>How can an agent use my API key without being able to steal it?</strong></summary>

<br>

You can't keep a secret from a process that runs arbitrary commands in the same namespace — so Comis
never puts it there. The real key lives only in the daemon's encrypted store (AES-256-GCM, the same
`SecretManager` that feeds MCP). When the agent drives a credentialed CLI, Comis launches it inside the
exec sandbox with a **placeholder** key and routes its HTTPS traffic through an in-process broker. The
broker terminates TLS with its own CA, matches the request against an allow-list, swaps the placeholder
for the real secret (`x-api-key` / `Authorization: Bearer` / query param), and forwards it. The key is
never a file, never an env var, never on the wire the sandbox can read.

On Linux, the credentialed sandbox runs with its network namespace isolated (`--unshare-net`) — the broker
unix socket is the only bind-mounted network path, making the broker the designed-to-be-only destination.
Validation of this kernel-enforcement on the Linux production host class is in progress.

The broker **fails closed** (no secret → 502, request never forwarded) and audits every session, injection,
and blocked-egress attempt on the event bus with a `traceId` you can grep.

Today this covers **API-key / bearer / query-param** auth (Anthropic and Finnhub today, more on the
catalog roadmap). OAuth / subscription-only CLIs are not yet supported. [Deep dive →](https://docs.comis.ai/security/credential-broker)

The `executor.broker` config path is the planned interface; daemon config integration is in progress.

```yaml
executor:
  broker:
    bindings:
      - preset: anthropic
        secretRef: ANTHROPIC_EXECUTOR_KEY
      - preset: finnhub
        secretRef: FINNHUB_API_KEY
```

</details>
```

CRITICAL checks before saving:
- No number > 2 appears for provider/preset count
- No "non-bypassable" or "kernel-locked egress" stated as a guarantee (use "designed-to-be-only destination" + "validation in progress")
- No latency/metric numbers
- The FAQ YAML example uses only the two preset entries (anthropic + finnhub)
- The <!-- publish gate --> comment is present in the Why Comis section
  </action>
  <verify>
    <automated>grep -c "publish gate: flip to Block 1 post-R1" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/README.md && grep -c "docs.comis.ai/security/credential-broker" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/README.md && grep -c "Anthropic and Finnhub today" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/README.md</automated>
  </verify>
  <done>README.md has all 4 broker additions. publish-gate comment present in Why Comis. Provider count is "Anthropic and Finnhub today" (exactly 2). Deep-dive URL is https://docs.comis.ai/security/credential-broker. No "non-bypassable" or "kernel-locked egress" guarantee language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → GitHub visitors | README is the first public impression of the project's security posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Information Disclosure | README Why Comis — containment claim | mitigate | Pre-P4 safe variant used; "validation in progress" qualifier; `<!-- publish gate -->` comment for future flip |
| T-07-13 | Information Disclosure | README FAQ — executor.broker config | mitigate | "daemon config integration is in progress" note in the FAQ block; condensed YAML shows schema contract only |
| T-07-14 | Information Disclosure | README provider count | mitigate | "Anthropic and Finnhub today" — exactly 2, no invented larger number |
</threat_model>

<verification>
After task completes:
1. `grep -c "publish gate: flip to Block 1 post-R1" README.md` → 1
2. `grep -c "docs.comis.ai/security/credential-broker" README.md` → ≥ 3 (Why Comis, Features, FAQ)
3. `grep -c "Anthropic and Finnhub today" README.md` → ≥ 1
4. `grep -v "^#\|<!--" README.md | grep -c "non-bypassable\|kernel-locked egress"` → 0 (no guarantee language)
5. `pnpm validate` still exits 0 (README changes do not affect build)
</verification>

<success_criteria>
- README.md has 4 broker additions (Why Comis, comparison table row, features cell, FAQ entry)
- Publish-gate comment present
- Provider count = 2 (Anthropic and Finnhub today) everywhere in the additions
- No "non-bypassable" or "kernel-locked egress" as guarantee
- No invented latency/metric numbers
- executor.broker FAQ config example is byte-identical condensed form (2 presets only)
- `pnpm validate` passes (docs-only change)
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation/07-05-SUMMARY.md`
</output>
