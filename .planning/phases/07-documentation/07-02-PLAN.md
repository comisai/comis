---
phase: 07-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - docs/security/exec-sandbox.mdx
  - docs/security/secrets.mdx
  - docs/security/audit.mdx
  - docs/reference/config-yaml.mdx
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "exec-sandbox.mdx documents the SandboxOptions.network modes (open/broker-only) and secureCredentialHome flag"
    - "secrets.mdx explains the broker as a second injection path alongside secretRefs"
    - "audit.mdx lists all 7 broker:* events in its event taxonomy table"
    - "config-yaml.mdx has the executor.broker section with the byte-identical YAML example and wiring-gap note"
    - "No new broken internal links introduced in any of these four files"
  artifacts:
    - path: "docs/security/exec-sandbox.mdx"
      provides: "SandboxOptions.network modes section, secureCredentialHome flag description"
      contains: "broker-only"
    - path: "docs/security/secrets.mdx"
      provides: "broker as second injection path; when to use broker vs secretRefs"
      contains: "credential-broker"
    - path: "docs/security/audit.mdx"
      provides: "broker:* event taxonomy table + secret:accessed + redaction-by-construction note"
      contains: "broker:session_opened"
    - path: "docs/reference/config-yaml.mdx"
      provides: "executor.broker bindings section with wiring-gap Warning"
      contains: "executor.broker"
  key_links:
    - from: "docs/security/exec-sandbox.mdx"
      to: "/security/credential-broker"
      via: "cross-reference link"
      pattern: "security/credential-broker"
    - from: "docs/reference/config-yaml.mdx"
      to: "executor.broker"
      via: "config section"
      pattern: "executor.broker"
---

<objective>
Update four core doc files to surface the credential broker where it belongs in each existing topic.

Purpose: Readers landing on exec-sandbox, secrets, audit, or the config reference should discover the broker. Each file gets a focused addition (one section or table) — not a rewrite.

Output: Four updated MDX files with broker content that cross-links to `/security/credential-broker`.
</objective>

<execution_context>
@/Users/mosheanconina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosheanconina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-documentation/07-RESEARCH.md
@.planning/phases/07-documentation/07-01-SUMMARY.md

<interfaces>
<!-- Key verified values — use exactly these, no invented numbers -->

SandboxOptions.network modes:
- { mode: "open" } or undefined → --unshare-all --share-net (default)
- { mode: "broker-only", brokerSocketPath: "/run/comis/broker.sock" } → --unshare-all --unshare-net --bind <socketPath> <socketPath>
Source: packages/skills/src/tools/builtin/sandbox/types.ts:33

secureCredentialHome: true removes:
- ~/.claude (RW bind)
- ~/.claude.json (RO bind)
- ~/.local/share/claude (RW bind)
Source: packages/skills/src/tools/builtin/sandbox/bwrap-provider.ts:196-218

broker:* event taxonomy (7 events):
broker:session_opened, broker:session_closed, broker:request, broker:injected,
broker:denied, broker:credential_unavailable, broker:egress_blocked
Source: packages/infra/src/credential-broker/broker-events.ts

secret:accessed event (separate from broker:*):
fields: secretName, agentId, outcome (success/not_found), timestamp
Source: packages/infra/src/credential-broker/mitm-broker.ts:501-506

broker:egress_blocked carries targetHostHash (SHA-256 hex) — NOT plaintext host.
Source: packages/infra/src/credential-broker/broker-events.ts:131-136

executor.broker YAML example — BYTE-IDENTICAL to 07-01-PLAN canonical block:
```yaml
# Note: executor.broker becomes active when daemon config wiring is integrated;
# the schema is shipped but the AppConfigSchema registration is pending.
executor:
  broker:
    bindings:
      - preset: anthropic
        secretRef: ANTHROPIC_EXECUTOR_KEY
      - preset: finnhub
        secretRef: FINNHUB_API_KEY
      - hostRules:
          - pattern: { kind: exact, host: my-internal-api.example.com }
            inject: []    # defaults to Authorization: Bearer
        secretRef: INTERNAL_API_TOKEN
```

executor.broker WIRING GAP: AppConfigSchema (schema.ts) has no executor key. executor.broker block would fail Zod strict validation today. Always include the Warning.

PUBLISH GATE: Never claim "non-bypassable" or "kernel-locked egress" as a validated guarantee in these files. Describe as architecture/design + "R1 pending".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update exec-sandbox.mdx and secrets.mdx</name>
  <files>docs/security/exec-sandbox.mdx, docs/security/secrets.mdx</files>
  <action>
Read both files first to understand their current structure and find the right insertion points.

**exec-sandbox.mdx additions:**

Find the section about bwrap sandbox profiles / SandboxOptions. Add a new section "## Network Modes and Credential Broker" (or append to the existing bwrap/sandbox profile section if one exists). Content:

The exec sandbox supports two network modes via `SandboxOptions.network`:

| Mode | bwrap args | Description |
|------|-----------|-------------|
| `open` (default) | `--unshare-all --share-net` | Standard sandbox; full network access |
| `broker-only` | `--unshare-all --unshare-net --bind <socketPath> <socketPath>` | Driven-CLI sandbox; only the broker unix socket is reachable via bind-mount |

The `broker-only` mode is set automatically for driven API-key CLI spawns. It is not a user-configurable option — the daemon sets it when it launches a credentialed CLI through the broker.

**Secure credential home** (`secureCredentialHome: true`): When set, the following mounts are omitted from the sandbox, so credential files are unreachable inside:
- `~/.claude` (RW)
- `~/.claude.json` (RO)
- `~/.local/share/claude` (RW)

On Linux, the `broker-only` network mode combines with `secureCredentialHome` so neither the key material nor a direct network path to the key provider is reachable inside the sandbox. Validation of network-level enforcement on the Linux production host class is in progress. [Credential Broker →](/security/credential-broker)

<Info>
Source: `packages/skills/src/tools/builtin/sandbox/types.ts` · `packages/skills/src/tools/builtin/sandbox/bwrap-provider.ts`
</Info>

---

**secrets.mdx additions:**

Read the file. Find where `secretRefs` is discussed (for exec tool environment injection). Add a section "## Broker injection (network boundary)" — or append a subsection to the existing exec/secretRefs coverage:

**Two injection paths for exec tools:**

| | `secretRefs` (env injection) | Credential broker (network injection) |
|---|---|---|
| **What it does** | Injects the secret as an env var inside the exec sandbox | Injects the secret at the network boundary (TLS termination) |
| **Secret visibility** | Env var is visible inside the sandbox (`echo $MY_KEY`) | Placeholder only inside sandbox; real key never enters |
| **Use when** | Simple scripts needing a key as an environment variable | API-key CLIs (Claude Code, curl with auth, Finnhub SDK) |
| **Platform** | All platforms | Linux broker-only egress is R1-gated; TLS injection works everywhere |

For step-by-step broker setup, see [Credential Broker →](/security/credential-broker).
  </action>
  <verify>
    <automated>grep -c "broker-only" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/docs/security/exec-sandbox.mdx && grep -c "credential-broker" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/docs/security/secrets.mdx</automated>
  </verify>
  <done>exec-sandbox.mdx contains "broker-only" in a network modes section. secrets.mdx contains a link to /security/credential-broker.</done>
</task>

<task type="auto">
  <name>Task 2: Update audit.mdx and config-yaml.mdx</name>
  <files>docs/security/audit.mdx, docs/reference/config-yaml.mdx</files>
  <action>
Read both files first.

**audit.mdx additions:**

Find the event taxonomy table/section. Add a new subsection "### Credential Broker Events" (under whatever heading covers broker/security events — e.g., after the existing secret:* events):

**broker:* event taxonomy**

The credential broker emits 7 typed events on the daemon event bus with redaction-by-construction — no secret value ever appears in any event payload:

| Event | Key payload fields |
|-------|--------------------|
| `broker:session_opened` | `sessionId`, `agentId`, `host`, `presetId?`, `timestamp` |
| `broker:session_closed` | `sessionId`, `agentId`, `durationMs`, `reason`, `timestamp` |
| `broker:request` | `sessionId`, `host`, `path`, `method`, `timestamp` |
| `broker:injected` | `sessionId`, `host`, `ruleKind`, `timestamp` |
| `broker:denied` | `sessionId`, `host`, `reason`, `statusCode`, `timestamp` |
| `broker:credential_unavailable` | `sessionId`, `secretRef`, `agentId`, `timestamp` |
| `broker:egress_blocked` | `sessionId`, `targetHostHash` (SHA-256 hex — NOT plaintext host), `timestamp` |

The `broker:egress_blocked` event deliberately carries `targetHostHash` (SHA-256 hex) rather than the plaintext hostname for redaction-by-construction — even blocked egress targets are not logged in plaintext.

The existing `secret:accessed` event is also emitted by the broker for each per-request SecretManager resolution, with fields: `secretName`, `agentId`, `outcome` (success/not_found), `timestamp`.

<Info>
Source: `packages/infra/src/credential-broker/broker-events.ts`
</Info>

---

**config-yaml.mdx additions:**

config-yaml.mdx is long (2051 lines). Read its structure to find the right insertion point — look for the `executor` top-level section or find the end of the configuration blocks. Add a new section "**Credential Broker Bindings (executor.broker.bindings[*])**" near the end of the configuration reference (after the existing execSandbox section if present, or at a logical position for executor-level config).

Add first:

<Warning>
**Daemon wiring in progress.** The `executor.broker` binding schema is shipped (`packages/core/src/config/schema-broker.ts`), but the `AppConfigSchema` integration is pending — adding an `executor:` block to `config.yaml` today will fail Zod strict-mode validation. The example below documents the planned config contract; it becomes active once the daemon wiring is integrated.
</Warning>

Then the config example (byte-identical to the canonical block from 07-01):

```yaml title="~/.comis/config.yaml"
# executor.broker.bindings — provider-agnostic; presets are optional sugar
executor:
  broker:
    bindings:
      # Option A: built-in preset — Anthropic (header injection)
      - preset: anthropic
        secretRef: ANTHROPIC_EXECUTOR_KEY

      # Option B: built-in preset — Finnhub (query param injection)
      - preset: finnhub
        secretRef: FINNHUB_API_KEY

      # Option C: custom binding — any host, no preset required
      # A binding with no 'inject' defaults to Authorization: Bearer
      - hostRules:
          - pattern: { kind: exact, host: my-internal-api.example.com }
            inject: []    # defaults to Authorization: Bearer
        secretRef: INTERNAL_API_TOKEN

      # Option D: custom binding with explicit header injection
      - hostRules:
          - pattern: { kind: suffix, suffix: .amazonaws.com }
            inject:
              - kind: setHeader
                name: x-amz-security-token
                format: raw
        secretRef: AWS_SESSION_TOKEN
```

Then the fields table:

**Credential Broker Bindings (executor.broker.bindings[*])**

| Key | Type | Required | Description |
|-----|------|----------|-------------|
| `preset` | `string` | one of preset/hostRules | Built-in preset ID (`anthropic`, `finnhub`) — expands to preset's host rules |
| `hostRules` | `HostRule[]` | one of preset/hostRules | Custom host rules (provider-agnostic) |
| `secretRef` | `string` | yes | SecretManager key resolved per-request; never cached to disk |
| `credentialRefs` | `Record<string, string>` | no | Extra refs for multi-field finalizers |

**HostRule fields (`hostRules[*]`):**

| Key | Type | Required | Description |
|-----|------|----------|-------------|
| `pattern` | `{ kind: "exact", host: string } \| { kind: "suffix", suffix: string }` | yes | Host match pattern |
| `inject` | `InjectionRule[]` | no | Injection rules; empty array defaults to `Authorization: Bearer` |
| `pathPrefix` | `string` | no | Restrict to requests with this path prefix |
| `pathPolicy` | `string` | no | Glob pattern for allowed paths |
| `finalizer` | `object` | no | Post-injection body transform (e.g., `awsSigV4` — no-op in this release) |

For full configuration details and examples, see [Credential Broker →](/security/credential-broker).
  </action>
  <verify>
    <automated>grep -c "broker:session_opened" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/docs/security/audit.mdx && grep -c "Daemon wiring in progress" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/docs/reference/config-yaml.mdx && grep -c "executor.broker" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/docs/reference/config-yaml.mdx</automated>
  </verify>
  <done>audit.mdx contains all 7 broker:* events in a table. config-yaml.mdx contains the executor.broker section with the wiring-gap Warning and the byte-identical YAML example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operators | Operators use config-yaml.mdx to write their config.yaml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | config-yaml.mdx executor.broker example | mitigate | `<Warning>` callout present: "executor.broker block fails Zod strict today"; explicit "AppConfigSchema integration pending" note — operator cannot accidentally break their config |
| T-07-06 | Information Disclosure | exec-sandbox.mdx network enforcement claim | mitigate | "R1 pending / validation in progress" framing; no "non-bypassable" language |
| T-07-07 | Spoofing | broken internal links | accept | All links root-relative; mintlify broken-links validates post-merge |
</threat_model>

<verification>
After both tasks:
1. `grep -c "broker-only" docs/security/exec-sandbox.mdx` → ≥ 1
2. `grep -c "credential-broker" docs/security/secrets.mdx` → ≥ 1
3. `grep -c "broker:session_opened" docs/security/audit.mdx` → ≥ 1
4. `grep -c "Daemon wiring in progress" docs/reference/config-yaml.mdx` → ≥ 1
5. `grep -c "executor.broker" docs/reference/config-yaml.mdx` → ≥ 1
6. From docs/: `mintlify broken-links` — no NEW broken links
</verification>

<success_criteria>
- exec-sandbox.mdx: network modes table (open/broker-only), secureCredentialHome flag removal list, cross-link to /security/credential-broker
- secrets.mdx: two-row comparison table (secretRefs vs broker), cross-link to /security/credential-broker
- audit.mdx: all 7 broker:* events in a table, targetHostHash redaction note, secret:accessed note
- config-yaml.mdx: wiring-gap Warning, byte-identical YAML example, HostRule fields table, cross-link to /security/credential-broker
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation/07-02-SUMMARY.md`
</output>
