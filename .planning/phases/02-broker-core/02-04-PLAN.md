---
phase: 02-broker-core
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - packages/infra/src/credential-broker/index.ts
  - packages/infra/src/index.ts
autonomous: true
requirements:
  - BROKER-01
  - BROKER-02
  - BROKER-03

must_haves:
  truths:
    - "The broker can be imported from @comis/infra by daemon/composition root without any internal path hacks"
    - "pnpm validate exits 0 — clean build, no cycles, lint:security clean, all coverage floors met"
    - "The @comis/infra architecture test still passes — no forbidden downstream imports in credential-broker/"
  artifacts:
    - path: "packages/infra/src/credential-broker/index.ts"
      provides: "Barrel re-exporting createSessionManager, createMitmBroker, and their public types"
      contains: "createSessionManager\|createMitmBroker"
    - path: "packages/infra/src/index.ts"
      provides: "credential-broker modules on the @comis/infra public surface"
      contains: "credential-broker/index.js"
  key_links:
    - from: "packages/daemon/src/"
      to: "@comis/infra"
      via: "import { createSessionManager, createMitmBroker } from '@comis/infra'"
      pattern: "createSessionManager|createMitmBroker"
    - from: "packages/infra/src/index.ts"
      to: "packages/infra/src/credential-broker/index.ts"
      via: "export from ./credential-broker/index.js"
      pattern: "credential-broker"
---

<objective>
Wire the credential-broker barrel into the @comis/infra public surface and run the full pnpm validate gate to confirm Phase 2 is shippable.

Purpose: Plans 02-02 and 02-03 created the implementation files but nothing exports them yet. This plan creates the barrel index.ts inside credential-broker/, adds one export line to infra/src/index.ts, and confirms all CI gates pass — build, cycles, lint:security, and coverage (infra floors: lines 95 / branches 92 / functions 100).

Output: credential-broker/index.ts; infra/src/index.ts updated; pnpm validate green.
</objective>

<execution_context>
@/Users/mosheanconina/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosheanconina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-broker-core/02-RESEARCH.md
@.planning/phases/02-broker-core/02-PATTERNS.md
@.planning/phases/02-broker-core/02-02-SUMMARY.md
@.planning/phases/02-broker-core/02-03-SUMMARY.md

<interfaces>
<!-- Confirmed patterns from PATTERNS.md for barrels. -->

From packages/infra/src/index.ts (current last lines):
```typescript
export { createSystemClock } from "./runtime/clock.js";
export { createSystemEnv } from "./runtime/env.js";
export { createSystemTimers } from "./runtime/timers.js";
```
Pattern: one export per adapter module. Append after the last line.

Names to export from credential-broker/index.ts:
- createSessionManager (value)
- SessionManager, SessionManagerDeps, IssuedSession, SessionInfo (types)
- createMitmBroker (value)
- MitmBrokerPort, MitmBrokerDeps (types)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create credential-broker/index.ts barrel</name>
  <files>packages/infra/src/credential-broker/index.ts</files>
  <read_first>
    - packages/infra/src/index.ts (the pattern for how infra barrels are written)
    - PATTERNS.md §packages/infra/src/credential-broker/index.ts (exact barrel pattern)
    - 02-02-SUMMARY.md (what session-manager.ts exports after GREEN)
    - 02-03-SUMMARY.md (what mitm-broker.ts exports after GREEN)
  </read_first>
  <action>
Create packages/infra/src/credential-broker/index.ts:

```typescript
// SPDX-License-Identifier: Apache-2.0
// @comis/infra — credential-broker barrel (Phase 2)

export { createSessionManager } from "./session-manager.js";
export type { SessionManager, SessionManagerDeps, IssuedSession, SessionInfo } from "./session-manager.js";

export { createMitmBroker } from "./mitm-broker.js";
export type { MitmBrokerPort, MitmBrokerDeps } from "./mitm-broker.js";
```

All .js extensions on imports (ESM with .ts source). `export type` for interfaces, bare `export` for factory functions.
  </action>
  <verify>
    <automated>grep -c "createSessionManager\|createMitmBroker" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/packages/infra/src/credential-broker/index.ts</automated>
  </verify>
  <done>File exists with both factory function exports and all four type exports. grep count = 2 (one createSessionManager, one createMitmBroker).</done>
</task>

<task type="auto">
  <name>Task 2: Add credential-broker to infra/src/index.ts public surface</name>
  <files>packages/infra/src/index.ts</files>
  <read_first>
    - packages/infra/src/index.ts (read the current last few lines to know exactly where to append)
    - PATTERNS.md §packages/infra/src/index.ts (addition pattern)
  </read_first>
  <action>
Append the following two lines to the END of packages/infra/src/index.ts:

```typescript
// Credential broker (MITM proxy runtime, Phase 2 — BROKER-01..03)
export { createSessionManager, createMitmBroker } from "./credential-broker/index.js";
export type { SessionManager, SessionManagerDeps, IssuedSession, SessionInfo, MitmBrokerPort, MitmBrokerDeps } from "./credential-broker/index.js";
```

Use the Edit tool — append only; do NOT modify any existing lines. The existing infra/src/index.ts must remain identical above the appended lines.
  </action>
  <verify>
    <automated>grep -c "credential-broker" /Users/mosheanconina/Projects/comisai/comis_dev_2/comis/packages/infra/src/index.ts</automated>
  </verify>
  <done>grep count = 2 (the two new export lines contain "credential-broker"). The file compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Run pnpm validate — the Phase 2 CI gate</name>
  <files></files>
  <read_first>Nothing — this is the full validation gate.</read_first>
  <action>
Run the full validation suite:

```bash
cd /Users/mosheanconina/Projects/comisai/comis_dev_2/comis && pnpm validate
```

This runs: build:clean (all 15 packages clean-room) → cycles (madge) → cycles:refs (tsc project refs) → lint:security (ESLint) → test:coverage (Vitest with infra floors: lines 95 / branches 92 / functions 100).

Expected outcome: all gates green. If any gate fails, diagnose and fix:

Coverage failures (most likely):
- "Coverage for lines (XX.XX %) does not meet global threshold (95)" → a branch in session-manager.ts or mitm-broker.ts is not covered. Add the missing test case to the appropriate test file, then re-run.
- Common uncovered branches: the endSession-unknown-id no-op path, the TTL-eviction boundary condition, the 8KB header-size-cap path in mitm-broker.
- To narrow: `cd packages/infra && pnpm vitest run --coverage src/credential-broker/` — check the per-file coverage report.

Architecture test failure:
- `@comis/infra -- architecture invariants` test fails → credential-broker/*.ts imported a forbidden downstream package (@comis/skills, @comis/agent, @comis/daemon, etc.). Remove the import and use only node:* built-ins, @comis/core, @comis/shared.

Lint:security failure:
- `no-process-env` → replace process.env access with EnvPort or SecretManager.
- `no-date-now` → replace Date.now() with deps.clock.now().
- `no-console` → replace console.log with deps.logger calls.

Cycle failure:
- `pnpm cycles:refs` TS6202 → there is a tsconfig.json project-reference cycle. Do NOT add a project reference to the credential-broker/ directory — it is part of infra/src/ which is already in the infra tsconfig.json.

The validate gate MUST exit 0 before Phase 2 is complete.
  </action>
  <verify>
    <automated>cd /Users/mosheanconina/Projects/comisai/comis_dev_2/comis && pnpm validate 2>&1 | tail -10</automated>
  </verify>
  <done>pnpm validate exits 0. All five gates pass: build:clean, cycles, cycles:refs, lint:security, test:coverage. Total test count equals or exceeds 26575 (Phase 1 baseline) plus all new broker tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @comis/infra public surface | createSessionManager and createMitmBroker are now importable by @comis/daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | barrel exports | accept | barrel re-exports only — no new logic introduced; no attack surface widened beyond what 02-02/02-03 already exposed |
| T-02-04-02 | Information Disclosure | pnpm validate coverage gate | mitigate | coverage gate at lines 95 / branches 92 / functions 100 enforces that every branch in the security-critical modules (407/403/502 paths) has a test; any uncovered branch fails CI |
</threat_model>

<verification>
After all three tasks:
1. `grep "credential-broker" packages/infra/src/credential-broker/index.ts` — file exists
2. `grep "credential-broker" packages/infra/src/index.ts` — two lines present
3. `pnpm validate` exits 0 with all five gates green
4. `pnpm vitest run packages/infra/src/__tests__/architecture.test.ts` — PASSES (no forbidden imports in credential-broker/)
5. Test count in validate output >= Phase 1 baseline (26575) + new broker tests
</verification>

<success_criteria>
- packages/infra/src/credential-broker/index.ts exists and exports createSessionManager, createMitmBroker plus all four type exports
- packages/infra/src/index.ts exports from ./credential-broker/index.js (both value and type exports)
- pnpm validate exits 0: build:clean, cycles (madge), cycles:refs (tsc), lint:security, test:coverage all green
- Infra coverage floors met: lines >=95, branches >=92, functions =100, statements >=95
- Architecture guard passes: no forbidden downstream imports in credential-broker/
</success_criteria>

<output>
After completion, create .planning/phases/02-broker-core/02-04-SUMMARY.md following the template at @/Users/mosheanconina/.claude/get-shit-done/templates/summary.md
</output>
