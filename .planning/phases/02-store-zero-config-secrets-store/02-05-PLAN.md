---
phase: 02-store-zero-config-secrets-store
plan: 05
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
files_modified:
  - docker/README-comis.md
  - docs/installation/install-render.mdx
  - docs/operations/docker.mdx
  - docs/security/secrets.mdx
  - docs/reference/environment-variables.mdx
  - CHANGELOG.md
autonomous: true
requirements:
  - STORE-04

must_haves:
  truths:
    - "All 5 docs files no longer describe the encrypted store as 'optional' or 'opt-in'"
    - "All 5 docs files reflect that the encrypted store is auto-generated on first boot (no manual key generation step required)"
    - "`COMIS_DISABLE_ENCRYPTED_SECRETS` is documented in environment-variables.mdx and README-comis.md as the escape hatch"
    - "CHANGELOG has a v1.1 entry under `## [Unreleased]` or a new `## [1.1.0]` section documenting the posture change as a behavior break"
    - "No doc still contains the phrase 'Skip SECRETS_MASTER_KEY' or 'Optional — set to enable'"
  artifacts:
    - path: "docker/README-comis.md"
      provides: "Updated SECRETS_MASTER_KEY description (auto-generated) + COMIS_DISABLE_ENCRYPTED_SECRETS documented"
      contains: "auto-generated"
    - path: "docs/installation/install-render.mdx"
      provides: "Removed 'Skip SECRETS_MASTER_KEY for a default deployment' tip"
      contains: "auto-generated"
    - path: "docs/operations/docker.mdx"
      provides: "Secrets section updated to reflect auto-init"
      contains: "encrypted-by-default"
    - path: "docs/security/secrets.mdx"
      provides: "Setup wizard replaced with auto-init description; backup obligation added"
      contains: "backup"
    - path: "docs/reference/environment-variables.mdx"
      provides: "`SECRETS_MASTER_KEY` description updated; `COMIS_DISABLE_ENCRYPTED_SECRETS` added"
      contains: "COMIS_DISABLE_ENCRYPTED_SECRETS"
    - path: "CHANGELOG.md"
      provides: "Behavior-break entry under Unreleased/1.1.0 for encrypted-by-default"
      contains: "behavior break"
  key_links: []
---

<objective>
Update 5 install docs and CHANGELOG to reflect the encrypted-by-default posture change
introduced by STORE-01, STORE-02, and STORE-03.

Purpose: The encrypted store was previously opt-in (required manual `SECRETS_MASTER_KEY`
setup). After this phase, it is opt-out (auto-generated on first boot, disabled only via
`COMIS_DISABLE_ENCRYPTED_SECRETS=1`). Four docs currently instruct operators to skip or omit
`SECRETS_MASTER_KEY` — this is now wrong. Operators need to know: (1) the key is
auto-generated, (2) they must back up `~/.comis/.env`, (3) they can opt out with
`COMIS_DISABLE_ENCRYPTED_SECRETS=1`, and (4) this is a deliberate behavior break.

Output:
- 5 updated doc files with accurate encrypted-by-default language
- CHANGELOG entry classifying this as a breaking behavior change
- STORE-04 acceptance criteria met
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-store-zero-config-secrets-store/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docker/README-comis.md and docs/installation/install-render.mdx</name>
  <files>docker/README-comis.md, docs/installation/install-render.mdx</files>

  <read_first>
    - docker/README-comis.md (full file — read once; lines 85 and 125 contain stale "optional" language)
    - docs/installation/install-render.mdx (full file — read once; lines 110-113 contain the "Skip SECRETS_MASTER_KEY" tip)
  </read_first>

  <action>
    ### docker/README-comis.md

    **Change 1 — docker-compose snippet comment (around line 85):**
    Replace:
    ```
    # Optional — set to enable the encrypted secrets.db (opt-in).
    ```
    With:
    ```
    # Auto-generated on first boot. Back up ~/.comis/.env (contains SECRETS_MASTER_KEY).
    # To opt out: set COMIS_DISABLE_ENCRYPTED_SECRETS=1
    ```

    **Change 2 — env var table (around line 125):**
    Replace the `SECRETS_MASTER_KEY` row:
    From: `| **Optional.** 32-byte hex key (generate with `openssl rand -hex 32`). When set, ... `
    To: `| Auto-generated on first boot and written to `~/.comis/.env` (mode 0600). Back up this file — losing the key makes `secrets.db` permanently unreadable. To opt out of the encrypted store: set `COMIS_DISABLE_ENCRYPTED_SECRETS=1`. |`

    **Change 3 — add COMIS_DISABLE_ENCRYPTED_SECRETS to the env var table:**
    After the `SECRETS_MASTER_KEY` row, add a new row:
    `| `COMIS_DISABLE_ENCRYPTED_SECRETS` | **Optional.** Set to `1` to skip auto-init and run in envfile-only mode. Emits a startup WARN. Use only if you need to manage secrets manually in `.env`. |`

    ### docs/installation/install-render.mdx

    **Change — remove the "Skip SECRETS_MASTER_KEY" tip (around lines 110-113):**
    Replace the tip block that says:
    ```
    Skip `SECRETS_MASTER_KEY` for a default deployment. Comis runs in legacy
    ...
    recommended setup for Render. Only add `SECRETS_MASTER_KEY` if you
    specifically need the encrypted-secrets-DB feature for `SecretRef` config
    ```
    With:
    ```
    The encrypted secrets store is **enabled by default**. On first boot, Comis
    auto-generates a master key and writes it to `~/.comis/.env` (mode 0600).
    **Back up this file** — losing `SECRETS_MASTER_KEY` makes `secrets.db`
    permanently unreadable. To disable the encrypted store (envfile-only mode),
    set `COMIS_DISABLE_ENCRYPTED_SECRETS=1`.
    ```

    Adapt the surrounding MDX structure (tip callout, admonition type, etc.) to match the
    existing formatting conventions in the file. Do not change the tip/callout component tag
    — just update its content.
  </action>

  <acceptance_criteria>
    - `grep -c "Optional.*set to enable\|opt-in\|Skip.*SECRETS_MASTER_KEY" docker/README-comis.md` returns 0
    - `grep -c "auto-generated\|Auto-generated" docker/README-comis.md` returns >= 1
    - `grep -c "COMIS_DISABLE_ENCRYPTED_SECRETS" docker/README-comis.md` returns >= 1
    - `grep -c "Skip.*SECRETS_MASTER_KEY\|legacy.*env.*mode\|opt-in" docs/installation/install-render.mdx` returns 0
    - `grep -c "auto-generated\|Auto-generated\|enabled by default" docs/installation/install-render.mdx` returns >= 1
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/mosheanconina/Projects/comisai/comis_dev_2/comis && grep -c "Optional.*set to enable\|opt-in\|Skip.*SECRETS_MASTER_KEY" docker/README-comis.md && grep -c "COMIS_DISABLE_ENCRYPTED_SECRETS" docker/README-comis.md && grep -c "Skip.*SECRETS_MASTER_KEY" docs/installation/install-render.mdx</automated>
    Expected: first command returns 0, second returns >= 1, third returns 0.
  </verify>

  <done>
    - docker/README-comis.md: stale "optional/opt-in" language replaced; auto-generated + backup + COMIS_DISABLE_ENCRYPTED_SECRETS documented
    - docs/installation/install-render.mdx: "Skip SECRETS_MASTER_KEY" tip replaced with encrypted-by-default notice
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/operations/docker.mdx, docs/security/secrets.mdx, docs/reference/environment-variables.mdx, and CHANGELOG.md</name>
  <files>docs/operations/docker.mdx, docs/security/secrets.mdx, docs/reference/environment-variables.mdx, CHANGELOG.md</files>

  <read_first>
    - docs/operations/docker.mdx (full file — read once; scan for any lines containing "optional", "opt-in", "Optional", or "Opt-in" near secrets/SECRETS_MASTER_KEY — these are the stale posture lines that must be updated, consistent with the concrete change targets in Task 1)
    - docs/security/secrets.mdx (full file — read once; the "Setting Up the Encrypted Store" wizard section is the primary change target)
    - docs/reference/environment-variables.mdx (full file — read once; find SECRETS_MASTER_KEY entry and check for COMIS_DISABLE_ENCRYPTED_SECRETS)
    - CHANGELOG.md (lines 1-40 — read once; identify the existing Unreleased section or latest version header)
  </read_first>

  <action>
    ### docs/operations/docker.mdx

    Find any section that describes the encrypted store as optional or requires a manual key.
    Update to reflect auto-init. Specifically:
    - Any sentence with "optional" + "SECRETS_MASTER_KEY" or "encrypted" → update to "enabled by default, auto-generated"
    - Any step like "Generate a key: `openssl rand -hex 32`" → replace with "The key is auto-generated on first boot. Back up `~/.comis/.env`."
    - Add a note about `COMIS_DISABLE_ENCRYPTED_SECRETS=1` if a disable/opt-out section exists.
    - The sentence "pushing vX.Y.Z produces..." in this file is illustrative and NOT bumped per CLAUDE.md.

    ### docs/security/secrets.mdx

    The "Setting Up the Encrypted Store" section currently has a step-by-step wizard for
    manually generating and setting `SECRETS_MASTER_KEY`. Replace the wizard section with:

    ```markdown
    ## Encrypted Store (Auto-Generated)

    Starting with v1.1, the encrypted secrets store is **enabled by default**.

    On first boot, Comis auto-generates a 32-byte master key and writes it to
    `~/.comis/.env` as `SECRETS_MASTER_KEY=<hex>` (file mode 0600, owner-read-only).

    **Backup obligation:** The key in `~/.comis/.env` is the only copy. Losing it makes
    `secrets.db` permanently unreadable (AES-256-GCM, no key escrow). Back up this file
    to a secure location immediately after first boot.

    ### Opting Out

    To run in envfile-only mode (no encrypted store):

    ```env
    COMIS_DISABLE_ENCRYPTED_SECRETS=1
    ```

    The daemon emits a startup WARN when opt-out is active.

    > **Behavior break (v1.1):** Previously, the encrypted store required manual setup.
    > Fresh installs now get encryption by default. Existing installs with no
    > `SECRETS_MASTER_KEY` will have one auto-generated on next restart.
    ```

    Preserve any other sections in the file (key rotation, backup verification, etc.) unchanged.

    ### docs/reference/environment-variables.mdx

    1. Find the `SECRETS_MASTER_KEY` entry. Update its description:
       - Remove: "Optional. Generate with `openssl rand -hex 32`."
       - Add: "Auto-generated on first boot and written to `~/.comis/.env` (0600). Back up this file. Providing this variable explicitly overrides the auto-generated value."

    2. Add `COMIS_DISABLE_ENCRYPTED_SECRETS` entry (after `SECRETS_MASTER_KEY` or in an appropriate alphabetical position):
       ```
       | `COMIS_DISABLE_ENCRYPTED_SECRETS` | `1` | Optional. Set to `1`, `true`, or `on` to disable auto-init and run in envfile-only mode. The daemon emits a startup WARN when active. |
       ```

    ### CHANGELOG.md

    Add to the existing `## [Unreleased]` section (or create one if it doesn't exist),
    under a `### Breaking Changes` sub-heading:

    ```markdown
    ### Breaking Changes

    - **Encrypted secrets store is now opt-out (was opt-in)** — On first boot with no
      `SECRETS_MASTER_KEY` set, Comis auto-generates a master key and writes it to
      `~/.comis/.env` (mode 0600). The encrypted store is usable immediately on the same
      boot with no restart required. **Action required for existing installs:** Back up
      `~/.comis/.env` — losing `SECRETS_MASTER_KEY` makes `secrets.db` permanently
      unreadable. To keep the prior behavior (no encrypted store), set
      `COMIS_DISABLE_ENCRYPTED_SECRETS=1`.
    ```

    Also add under `### Added`:
    ```markdown
    - `COMIS_DISABLE_ENCRYPTED_SECRETS=1` env flag — opts out of auto-init; daemon boots in
      envfile-only mode with a startup WARN.
    - `env_set` now returns an immediate `secrets_store_unavailable` error (with actionable
      hint) when the encrypted store is not configured — no confirmation dance, no rate-limit
      consumption.
    ```
  </action>

  <acceptance_criteria>
    - `grep -iv '^#' docs/operations/docker.mdx | grep -ic "optional.*secret\|opt-in.*secret"` returns 0
    - `grep -c "COMIS_DISABLE_ENCRYPTED_SECRETS" docs/security/secrets.mdx` returns >= 1
    - `grep -c "backup\|Back up\|back up" docs/security/secrets.mdx` returns >= 1
    - `grep -c "auto-generated\|Auto-generated\|auto.generated" docs/security/secrets.mdx` returns >= 1
    - `grep -c "COMIS_DISABLE_ENCRYPTED_SECRETS" docs/reference/environment-variables.mdx` returns >= 1
    - `grep -c "COMIS_DISABLE_ENCRYPTED_SECRETS" CHANGELOG.md` returns >= 1
    - `grep -c "behavior break\|Breaking Change\|breaking change\|opt-out.*was opt-in\|Breaking" CHANGELOG.md` returns >= 1
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/mosheanconina/Projects/comisai/comis_dev_2/comis && grep -c "COMIS_DISABLE_ENCRYPTED_SECRETS" docs/security/secrets.mdx docs/reference/environment-variables.mdx CHANGELOG.md && grep -c "behavior break\|Breaking" CHANGELOG.md</automated>
    Expected: each file shows >= 1 match for COMIS_DISABLE_ENCRYPTED_SECRETS; CHANGELOG shows >= 1 for breaking change marker.
  </verify>

  <done>
    - docs/operations/docker.mdx: stale opt-in language removed; auto-generated described
    - docs/security/secrets.mdx: wizard replaced with auto-init description + backup obligation + opt-out section + behavior-break callout
    - docs/reference/environment-variables.mdx: SECRETS_MASTER_KEY description updated; COMIS_DISABLE_ENCRYPTED_SECRETS entry added
    - CHANGELOG.md: Breaking Changes entry + Added entries for v1.1 encrypted-by-default posture change
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc content → operator behavior | Docs guide operators to back up key material; incorrect docs could lead to key loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | Docs revealing that `~/.comis/.env` holds the master key | accept | This is a public file path documented in Comis install guides; operators need to know the location to back it up; the path is already documented in existing docs |
| T-02-05-02 | Denial of Service | Operator follows old docs (opt-in), skips key setup, gets store = unavailable | mitigate | This plan explicitly replaces all "skip SECRETS_MASTER_KEY" guidance; the behavior-break CHANGELOG entry plus the auto-generated notice closes this gap |
</threat_model>

<verification>
After both tasks complete:
1. `grep -rn "Optional.*set to enable\|opt-in" docker/README-comis.md docs/installation/install-render.mdx docs/operations/docker.mdx docs/security/secrets.mdx docs/reference/environment-variables.mdx` — must return 0 results
2. `grep -rn "COMIS_DISABLE_ENCRYPTED_SECRETS" docker/README-comis.md docs/reference/environment-variables.mdx docs/security/secrets.mdx CHANGELOG.md` — must return >= 1 per file
3. `grep -c "backup\|Back up" docs/security/secrets.mdx` — must return >= 1
4. `grep -c "behavior break\|Breaking" CHANGELOG.md` — must return >= 1
5. `grep -c "Skip.*SECRETS_MASTER_KEY" docs/installation/install-render.mdx` — must return 0
</verification>

<success_criteria>
- All 5 docs files reflect encrypted-by-default (no "optional", no "skip", no "opt-in")
- `COMIS_DISABLE_ENCRYPTED_SECRETS` documented in at least docker/README-comis.md, docs/security/secrets.mdx, and docs/reference/environment-variables.mdx
- CHANGELOG has a Breaking Changes entry for v1.1 encrypted-by-default posture change
- Backup obligation mentioned in docs/security/secrets.mdx
- `pnpm validate` still passes (docs changes do not affect TypeScript build)
</success_criteria>

<output>
After completion, create `.planning/phases/02-store-zero-config-secrets-store/02-05-SUMMARY.md`
</output>
