---
phase: 05-forced-egress-secure-credential-home
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/skills/src/tools/builtin/sandbox/bwrap-egress-integration.test.ts
autonomous: true
requirements:
  - EGRESS-01
  - EGRESS-02

must_haves:
  truths:
    - "The R1 spike GO/NO-GO harness exists as a Linux-gated test file that skips on macOS"
    - "The test file captures the three GO criteria: rootless namespace creation, socket bind-mount reachability, direct egress blocked"
    - "On macOS the entire describe block is silently skipped (skipIf pattern) — no false failures in local CI"
    - "The live GO/NO-GO is explicitly marked OUTSTANDING until confirmed on the Linux production host class"
  artifacts:
    - path: "packages/skills/src/tools/builtin/sandbox/bwrap-egress-integration.test.ts"
      provides: "Linux-gated R1 spike integration harness + live no-route + files-absent tests"
      contains: "describe.skipIf"
  key_links:
    - from: "bwrap-egress-integration.test.ts"
      to: "packages/skills/src/tools/builtin/sandbox/bwrap-provider.ts"
      via: "BwrapProvider import"
      pattern: "canEgressIntegrationRun"
---

<objective>
Author the Linux-gated R1 spike integration harness. This is the first deliverable of Phase 5
and the hard gate for the entire milestone: if rootless --unshare-net + broker-only routing is
unachievable on the production host class, the milestone stops.

This plan creates ONLY the test file — no production code changes. The test file:
- Skips entirely on macOS (describe.skipIf pattern, mirroring exec-tool.test.ts:1439)
- On Linux: asserts the three R1 GO criteria (namespace creation, socket bind-mount, egress blocked)
- Asserts live EGRESS-02: no credential files reachable inside the unshared namespace
- Is the validation harness that must run on the Linux production host class before Phase 7
  containment claims are published

HARD GATE: The file is authored here. The GO/NO-GO is OUTSTANDING until run on the Linux
production host class. Phase 7 docs (containment claims "non-bypassable", "kernel-locked egress")
are publish-gated on this test passing on that host.

Purpose: Separate the harness authoring (macOS-possible) from the live spike execution
(Linux-only). The executor can author and commit a syntactically valid, platform-gated test
on any platform; the live result is separate.

Output: bwrap-egress-integration.test.ts — a complete, skipped-on-darwin integration test
file ready to be run on the Linux production host class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-forced-egress-secure-credential-home/05-RESEARCH.md
@.planning/phases/05-forced-egress-secure-credential-home/05-PATTERNS.md
@.planning/phases/05-forced-egress-secure-credential-home/05-VALIDATION.md

<read_first>
Read:
- packages/skills/src/tools/builtin/exec-tool/exec-tool.test.ts lines 1408-1445 (canRealBwrapSandbox gate + skipIf pattern — copy exactly)
- packages/skills/src/tools/builtin/sandbox/bwrap-provider.ts lines 128-144 (BwrapProvider.available() — used in gate function)
- packages/infra/src/credential-broker/mitm-broker.ts lines 714-750 (broker.start() returns port — integration test starts a real broker)
</read_first>

<interfaces>
<!-- Gate function pattern from exec-tool.test.ts:1412-1439 -->
```typescript
// Phase 5 gate (NO opt-in env flag — this IS the validation harness, not a dev-only test)
function canEgressIntegrationRun(): boolean {
  if (process.platform !== "linux") return false;
  // eslint-disable-next-line no-restricted-syntax -- Integration gate, Linux only
  const provider = new BwrapProvider();
  return provider.available();
}
const egressIntegrationAvailable = canEgressIntegrationRun();

describe.skipIf(!egressIntegrationAvailable)(
  "R1 spike: rootless --unshare-net broker-only egress (Linux only)",
  () => { ... }
);
```

<!-- R1 GO criteria (from RESEARCH.md) -->
// GO criterion 1: bwrapSmokeTest already validates namespace creation (BwrapProvider.available())
// GO criterion 2: Unix socket bind-mount works inside --unshare-net
// GO criterion 3: Direct TCP egress fails inside --unshare-net

<!-- PATTERNS.md Phase 5 gate difference -->
// Key: The existing exec-tool.test.ts gate uses COMIS_DEV_SANDBOX_INTEGRATION !== "1" because
// those tests hit the public network. The R1 spike uses only the local broker — no env flag needed.
</interfaces>
</context>

<feature>
  <name>R1 spike Linux-gated integration test harness</name>
  <files>
    packages/skills/src/tools/builtin/sandbox/bwrap-egress-integration.test.ts
  </files>
  <behavior>
The file must contain:

1. SPDX header: `// SPDX-License-Identifier: Apache-2.0`

2. `canEgressIntegrationRun()` gate function:
   - Returns false if `process.platform !== "linux"` (no Darwin execution)
   - Returns false if `new BwrapProvider().available()` is false
   - No opt-in env flag (R1 tests use only the local broker, not the public network)
   - Includes `// eslint-disable-next-line no-restricted-syntax -- Integration gate, Linux only` comment

3. `describe.skipIf(!egressIntegrationAvailable)("R1 spike: rootless --unshare-net broker-only egress (Linux only)", ...)` top-level suite

4. Inside the suite, three test groups:

   **Group A: R1 GO criterion 2 — Unix socket bind-mount**
   - Creates a Node `net.Server` listening on a temp Unix socket path (use `systemNowMs()` for uniqueness, NOT `Date.now()`)
   - Spawns bwrap with `--unshare-all`, `--unshare-net`, `--bind <socketPath> <socketPath>`, minimal system binds
   - Inside the namespace: connects to the socket, sends a CONNECT request, asserts a response is received
   - Asserts: socat (or nc) connects to the socket inside the namespace without ENOENT/EACCES
   - Cleans up the socket file in afterEach

   **Group B: R1 GO criterion 3 — Direct egress blocked**
   - Spawns bwrap with `--unshare-all`, `--unshare-net`, minimal binds (no socket)
   - Inside: `curl --max-time 2 https://example.com` or `wget --timeout=2 https://example.com`
   - Asserts: exit code != 0 AND stderr/stdout contains "Network is unreachable" or "curl: (6)" or similar network failure indicator
   - This is the live proof that `--unshare-net` isolates the network namespace

   **Group C: EGRESS-02 live — credential files absent**
   - Spawns bwrap with `--unshare-all`, `--unshare-net`, `secureCredentialHome:true` args
   - (Specifically: NO `--bind ~/.claude ...` and NO `--ro-bind ~/.claude.json ...` in the args)
   - Inside: attempts `cat ~/.claude/.credentials.json`, asserts exit code != 0 (file not found)
   - Inside: attempts `ls ~/.claude/`, asserts exit code != 0 or empty (dir not present)

5. Test timeouts: 15_000ms per test (bwrap + network ops can be slow)

6. `afterAll`: assert that no socket files from this test run remain in /tmp or /run/comis

7. NO `Date.now()` anywhere — use `systemNowMs()` from `@comis/core` for temp path uniqueness.

8. The describe block must have a clearly visible comment at the top:
   ```
   // R1 SPIKE GO/NO-GO: This suite is the Phase 5 hard gate.
   // Status: OUTSTANDING — must be run on the Linux production host class.
   // Phase 7 containment claims are publish-gated on this suite passing.
   ```
  </behavior>
  <implementation>
Import: `BwrapProvider` from `./bwrap-provider.js`; `describe`, `it`, `afterEach`, `afterAll`,
`expect` from `vitest`; `spawnSync`, `execFileSync` from `node:child_process`; `net` from `node:net`;
`unlinkSync`, `existsSync` from `node:fs`; `systemNowMs` from `@comis/core`.

The test file must compile without errors on macOS (even though the suite is skipped).
Use `spawnSync('bwrap', [...], { encoding: 'utf8', timeout: 15_000 })` for the actual bwrap spawns.
For the socket test, create the server before spawning bwrap; clean up in afterEach.
For the curl test, check `result.status !== 0` AND check stderr for network error indicators.
The credential-files-absent test needs to construct bwrap args that mirror the secure profile
(no claude binds) — import the BwrapProvider and construct opts with `secureCredentialHome: true`
and `network: { mode: "broker-only", brokerSocketPath: "/tmp/nonexistent.sock" }` so the args
are generated by the same production code path, then spawn those args directly.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → Linux kernel | Integration tests invoke real bwrap; incorrect namespace isolation would be a security failure |
| test process → socket file | Socket files created in /tmp must be cleaned up to prevent cross-session leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Elevation of Privilege | R1 GO criteria not validated | mitigate | This plan creates the validation harness; GO/NO-GO is confirmed on prod host before Phase 7 docs publish |
| T-05-02-02 | Information Disclosure | Socket files left on disk after test | mitigate | afterEach + afterAll unlink socket files; use per-test unique paths via systemNowMs() |
| T-05-02-03 | Spoofing | False GO (macOS test framework runs skipped tests) | mitigate | describe.skipIf pattern mirrors exec-tool.test.ts:1439; macOS run confirms SKIP, not PASS |
</threat_model>

<verification>
On macOS (confirm skipping, not false passes):
```bash
pnpm vitest run packages/skills/src/tools/builtin/sandbox/bwrap-egress-integration.test.ts 2>&1 | grep -E "skip|SKIP|0 tests"
```
Must show the suite was skipped (not 0 failures + 0 passes from running).

Compile check (macOS):
```bash
pnpm --filter @comis/skills tsc --noEmit 2>&1 | head -20
```
Must pass with 0 errors (file must compile even on macOS where BwrapProvider exists but bwrap isn't present).

On Linux production host class (OUTSTANDING — this is the hard gate):
```bash
pnpm vitest run packages/skills/src/tools/builtin/sandbox/bwrap-egress-integration.test.ts
```
All three groups must PASS. This is the Phase 5 hard gate confirming EGRESS-01 + EGRESS-02 live.
</verification>

<success_criteria>
- bwrap-egress-integration.test.ts exists with SPDX header and the three test groups
- canEgressIntegrationRun() returns false on macOS (process.platform !== "linux")
- describe.skipIf pattern mirrors exec-tool.test.ts:1439 exactly
- `pnpm --filter @comis/skills tsc --noEmit` passes on macOS
- Suite skips (not errors) on macOS in pnpm vitest run
- Test file includes the R1 SPIKE GO/NO-GO comment and systemNowMs() (not Date.now())
- R1 live GO/NO-GO is marked OUTSTANDING in the phase summary — not claimed as passed on macOS
</success_criteria>

<output>
After completion, create `.planning/phases/05-forced-egress-secure-credential-home/05-02-SUMMARY.md`
Include: confirm the file compiles + skips on macOS, the three test group names, the R1 spike
OUTSTANDING status (not claimed as passed), and the command to run on the Linux production host.
</output>
