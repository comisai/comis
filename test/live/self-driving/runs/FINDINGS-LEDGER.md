# FINDINGS LEDGER — cross-run open/fixed tracker

> The persistent record so a NEW run starts with "what's still open," not a re-discovery. Findings D + E
> below were carried (still open) from the prior M1 run into the 2026-06-24 M1 re-run and had to be
> re-found by hand — this ledger exists to prevent that.
>
> **Protocol:** at STEP 1 of a run, read this ledger + grep the target's surface for any OPEN finding it
> touches (re-verify status at HEAD — a finding may have been fixed/merged since). At STEP 6, append any
> new finding and update the status of every one you re-touched. **A finding leaves this ledger only when
> FIXED + MERGED (cite the commit/PR) or proven obsolete (cite why).**
> Also: a prior run may live in the **legacy** `.planning/live-tests/<TARGET>-test-plan.md`, not just
> `self-driving/runs/` — check both.

## SESSION 2026-06-24b — root-cause + fix + reproduce sweep (branch `fix/telegram-media-resolver-apiroot`, NOT pushed to main; USER review/merge)

Every OPEN **product** finding below was driven to a test-first fix this session. The trust-tier security
change + the session-lifecycle fixes were re-verified **LIVE on the VPS via ground truth** (not chat replies).
Local gates green: `build`, `cycles:refs`, `lint:security` (0 errors), full per-package test suites
(daemon 5547, agent 8441, core/obs/gateway 7459, skills). Status per finding:

| id | resolution | commit | verified |
|---|---|---|---|
| **E** (whoami Unknown-method) | register `capabilities.introspect` unconditionally → disabled-state | `4031428f` | unit (109) |
| **F-OBS-1(b)** (fleet over-counts) | window activeChannels/activeAgents by row ts | `0905b50f` | unit (50) |
| **V1-NO-VISION** (/v1 array content) | accept multimodal content + named vision error | `d0514c14` | unit (20) |
| **COMPACT-KEY** (agent guesses key) | self-resolve `_callerSessionKey` ("self"/omit) | `f7675317` | unit + **LIVE** |
| **RELAX-SURFACE** (sandboxNoDowngrade silent) | boot `config_posture` signal + fleet finding | `c90afdbd` | unit (74) |
| **F-OBS-2** (web_search not on trajectory) | content-free {resultCount,domains} on tool.result | `f1be7194` | unit (277) |
| **P0-A-OBS** (budget pre-check no event) | emit subagent:budget_exceeded on the finishReason path | `25b4ebd9` | unit (46) |
| **D** (spawn-tree omits graph children) | `graph:node_spawned` → spawnTree leaf | `33a4576c` | unit (377) |
| **MANAGE-DEAD + MEM-DELETE-DEAD** | **trust-tier deny-by-origin** — admin-trust agent inherits admin (surface-exclusion attempt reverted `fdb947fe` per USER) | `f10947a8` | unit (5547) + **LIVE** (agents_manage success:true, no deny) |
| **COMPACT-STORE-MISS** (NEW — live repro) | lifecycle RPCs read EITHER store (file JSONL, not the empty SQLite) | `398163d4` | unit + **LIVE** (compact success:true) |
| **GIANT-INPUT-WEDGE NPE** (NEW — follow-on) | guard `msg.text?.length` (undefined-text NPE) | `4410f458` | unit (276) |
| GIANT-INPUT-WEDGE / MEDIA-INPUT-SSRF / MEM-IMPORT-DUP / MEDIA-TYPE / TELEGRAM-RESOLVER / OBS-SELF-DEAD | (prior-session commits on this branch) | see FIXED below | — |

**Residual OPEN** = provider-coverage gaps only (cannot reproduce on the Anthropic rig): **BUDGET-02-COV**, **A/B/C (Codex)**.
F-RIG-1 is rig-harness (DONE in-run). The OPEN table below is the pre-session snapshot; the rows above supersede it.

## OPEN
| id | finding | class | sev | source run | fix direction |
|---|---|---|---|---|---|
| **STREAM-02-DEAD** | M2 Phase 221 `later-queue` (push-completion + `'later'`-priority between-turns queue, the "no polling token-burn" pacing) is **built-but-not-wired**: `packages/agent/src/executor/later-queue.ts` (230 lines, fully unit-tested — `later-queue.test.ts` green) has **ZERO call-sites** outside its own test (`createLaterQueue`/`enqueue` ungrep-able in the executor/completion loop). STREAM-01 (order-preserving parallel) + STREAM-03 (`sleep` tool, live-confirmed) ARE wired; STREAM-02 is not. So the spec's STREAM-02 requirement is not LIVE. NOT a HARD-oracle failure (security/honesty all green) — an efficiency-pacing gap | built-but-not-wired (#1 class) | med | m2-durable-unattended-cli-20260624 (221) | wire `createLaterQueue` into the completion loop's push-completion path (where between-turn 'later' work is scheduled); add a **wiring-guard arch test** (the `audio-wiring-guard.test.ts` pattern) asserting a live call-site exists (RED: grep the executor build for `createLaterQueue(` → ≥1 non-test call-site; currently 0). Verify-then-fix: confirm no dynamic/indirect registration first. Structural (threads a queue through the hot completion loop) → documented, not rushed |
| **EVICT-MAP-LEAK** | M2 Phase 217 evict/denial-breaker per-`rootRunId` maps are NOT cleared on **normal run-end** (only on the trip/kill path) — `rpc-dispatch.ts:624` carries the team's own dated TODO(2026-06-24, T-217-06/T-217-15): "the chokepoint is per-call and has no clean run-end hook; the durable run-lifecycle / sessionEnd seam is the right place to drop a completed root's breaker counter + evict flag so the in-memory maps cannot grow under a storm of per-cron-fire roots." Confirmed present in deployed f1224dc8 | resource-leak (unbounded map growth) | low | m2-durable-unattended-cli-20260624 (217) | wire `evictRegistry.clear(rootRunId)` + `denialBreaker.evict(rootRunId)` on normal run-completion (the `setup-durable-resume`/runner sessionEnd seam named in the TODO). RED: simulate N completed roots → assert the breaker/evict maps don't retain entries for completed roots |
| **D** | `comis explain` spawn-tree shows the root but omits graph **sub-agent children** as leaves (`spawnTree:[root only]` though the children spawned — `web_search ok:28` from 4 analysts) | obs completeness (TREE-01) | med | M1 (prior + 2026-06-24) | bridge graph sub-agent spawn events into `assembleIncidentReportFromSources` so children reconstruct with per-node caps/budget/leaseId |
| **E** | `whoami`/`capabilities.introspect` under `autonomy.profile: assistant` returns **"Unknown RPC method"** (autonomy RPCs unregistered when disabled) instead of a clean `{enabled:false, caps:[]}` | honesty (PROFILE-05 adjacent) | low | M1 (prior + 2026-06-24) | register introspect unconditionally → return a disabled-state, not a method-error |
| **P0-A-OBS** | a per-node-budget-bounded node fails with graph-layer `error:""` + `tokensUsed:0` + **no `subagent:budget_exceeded` event** — the agent-layer `finishReason:"budget_exceeded"` is dropped at the completion-event boundary (BudgetGuard pre-check preempts the post-hoc breach). OE-H1 itself PASSES; this is the headline P0-A *observability* not firing live | obs (structural, 5-file) | med | orchestration-excellence-20260624-deepdrive | thread `finishReason` through the graph completion event (`graph-concurrency.ts:81`) → emit `subagent:budget_exceeded` + attribute the node error on the budget-abort path in `applyNodeBudgetBreach` |
| **MANAGE-DEAD** | `agents_manage` (+7 other `*_manage`) are **dead on the top-level agent surface** — exposed but their backing admin RPC is ORIGIN-01-denied → the agent burns a turn discovering the deny; degrades DAG turns (`completed_with_tool_errors`) | origin-gating asymmetry (verdict C — **intended** deny, dead-tool UX) | low-med | orchestration + M1 | optional symmetric-exclusion: filter ADMIN_METHODS-backed `*_manage` tools out of the top-level surface at `setup-tools.ts:642-664` (mirror `SUB_AGENT_TOOL_DENYLIST`); pin with an arch test (MD-02 template) |
| **RELAX-SURFACE** | `security.agentToAgent.sandboxNoDowngrade:false` (a relaxed security default) is **not surfaced** at boot (no `config_posture`/WARN); the degrade-honesty Track-M rule wants relaxed defaults to surface | Track-M relaxation-surfacing | low | orchestration-excellence-20260624-deepdrive | emit a `config_posture` finding / boot WARN when the no-downgrade invariant is disabled |
| **BUDGET-02-COV** | BUDGET-02 zero-price limb (unknown/zero per-token pricing → wall/token limb, not $0) **not live-exercised** — Anthropic prices nominally so the $-limb binds first | coverage gap (provider-required) | n/a | M1 | drive on a Codex/subscription zero-price model; the wall/token limb is unit-tested |
| **A/B/C** (Codex) | (A) silent model-substitution for an unresolvable configured model · (B) `registerProvider` clobbers built-in OAuth providers · (C) viable-floor hint recommends a harmful knob for built-in OAuth | provider/SDK (Codex-specific) | varies | legacy M1 run | not re-exercised on Anthropic; see the legacy `SECURE-AGENT-AUTONOMY-M1-test-plan.md` §6 |
| **F-OBS-1** | `fleet.activity.activeChannels`/`activeAgents` are NOT time-windowed to `sinceHours` — `fleet-session-index.ts:240` adds every `session_started` channel from the whole-day `session-index.<date>.jsonl` regardless of ts, so a 1-session window reported 39 channels + a prior-run `downgrader` agent. Headline metrics (`sessions.total`/`degraded`/`breaker`/`cost`) ARE correctly windowed. Compounded by `clean-restart.sh`'s `rm -f logs/*.log` missing `session-index.*.jsonl` + `cache-trace.jsonl` (34MB) | obs windowing + harness clean-slate | low | 30uc-20260624 (Phase 0) | (a) DONE in-run: clean-restart.sh now also clears `*.jsonl*`+cache-trace+graph-runs+subagent-results; (b) TODO test-first: window the enumeration in `fleet-session-index.ts` to `sinceHours` (RED: a `session_started` older than the window must not appear in `activeChannels`). UC-30 reconciles on windowed metrics+trajectory, NOT activeChannels |
| **MEM-DELETE-DEAD** | `memory_manage` (the agent's forget/delete tool) calls `rpcCall("memory.delete")`, but `memory.delete` is `scopes:["admin"]` + a bespoke `_trustLevel!=="admin"` handler check → `assertNotAgentOrigin` denies it from agent origin (`reason:agent_origin_admin`). So the **user-sovereignty "forget me" path is dead from the agent surface** AND the turn **degrades** (`memory_manage:{ok:0,failed:2}`, `degraded:true`). The tool is *built agent-usable* (its own `createTrustGuard`+approval flow, `memory-manage-tool.ts:142/205`), so the deny preempts the tool's intended gate. Sibling RPCs `memory.flush`/`memory.browse`/`memory.stats` are also admin-scoped (browse is even commented "agent-level read"). Same class as MD-02 (`memory.store` was admin→rpc) BUT **delete is destructive+irreversible** so the obvious re-scope is security-sensitive. **NOT a false success** — the agent reported the limitation honestly (UC-02 forget = fails-honestly, no false-green) | origin-gating asymmetry (MD-02 class, **security-sensitive** — destructive op) | med | 30uc-20260624 (UC-02) | needs DESIGN (don't rush a blanket re-scope): EITHER (a) add a **narrow self-scoped agent delete** — delete only `source_session_key==caller` rows, gated by the tool's existing approval (respects "forget what I told you" without opening bulk-delete to injected turns); OR (b) MANAGE-DEAD symmetric-exclusion: filter `memory_manage` off the agent surface so it returns an honest "not available" without 2 failed calls + a degrade. RED: an agent-origin `memory_manage` forget on the caller's own memory succeeds (path a) / is absent not denied-mid-turn (path b). Pin with the `agent-memory-tools-deny-by-origin.test.ts` arch test (which currently has a blind spot — it didn't cover `memory.delete`) |
| **GIANT-INPUT-WEDGE** ✅**FIXED `00b53df0`+reproduced** (cap inbound msg >1M chars before scan/ingest + bound scan to 64KB; 4.5MB inject now honest-rejects in 8s, daemon responsive) | A single ~4.5MB / ~1.1M-token inbound message **wedges the entire daemon**: after `InputSecurityGuard scan complete` + dequeue, the agent-turn processing of the giant input (LCD ingest / tokenization / embedding over 4.5MB) **blocks the Node event loop** → daemon logs nothing for 8.5+ min, **gateway RPC times out** (event loop frozen), 46% CPU / 1.24GB RSS, ALL sessions+turns unresponsive (a fresh turn also hung). Supervisor can't recover it (process wedged, not exited) → manual kill+clean-restart needed. DoS/robustness: no daemon-side input-size guard before expensive SYNC processing. (Real Telegram caps text at 4096 chars so NOT reachable via normal TG text — but reachable via other channels/media-captions/the `/v1` API; defense-in-depth gap) | robustness / DoS (event-loop block) | **HIGH** (daemon-wide freeze) | 30uc-20260624 (UC-09 over-cap) | (a) daemon: cap inbound message bytes BEFORE the sync injection-scan/LCD-ingest path (reject/truncate >N MB with an honest error) + make the multi-MB LCD-ingest/embedding async/chunked so it can't block the event loop; find the exact sync hot path (likely the embedding chunker or a regex over the whole blob). RED: a >N-MB inbound message returns an honest size-error and the gateway stays responsive. (b) emulator-fidelity: enforce Telegram's 4096-char text cap on `/control/.../messages` injects (real TG would reject) — note `text` length at the boundary |
| **MEDIA-INPUT-SSRF** | (part 2 of the media-input chain; part 1 = TELEGRAM-RESOLVER-APIROOT, FIXED below). With the download URL fixed to honor `apiRoot`, the SSRF-guarded fetch (`telegram-resolver.ts:99`) now correctly **rejects the loopback/private apiRoot host** ("URL validation rejected" for `http://127.0.0.1:38411/file/…`). So media-INPUT through a **self-hosted local Bot API server on a private IP** (Telegram's documented large-file/privacy deployment) — and the test emulator — is blocked: getFile resolves but the byte download is SSRF-denied. Blocks UC-05/16/18/19 media-input live on the loopback rig | security-sensitive (SSRF guard ↔ trusted-endpoint) | med | 30uc-20260624 (UC-05) | thread the operator-configured channel `apiRoot` host into the **media** SSRF fetcher's allowlist (the `createSsrfGuardedFetcher` allow-loopback+explicit-allowlist mode already used by `local-stt-probe.ts:138`). MUST be **host-scoped to the configured apiRoot only** (NOT blanket loopback — else UC-10's `127.0.0.1:4766` block regresses). RED: a media fetch to the configured apiRoot host succeeds while an arbitrary loopback URL stays blocked. Verify vision capability + HARD injection-resist via the `/v1` API image path meanwhile (no Telegram resolver) |
| **COMPACT-KEY** | Agent-driven "compact our conversation" → `sessions_manage` constructs the WRONG session key: `default:678314278:telegram:678314278` (guesses `:telegram:`) but the real key is `default:678314278:678314278:peer:678314278` (`:peer:`) → `errorKind:dependency` "Session not found" → agent can't self-compact. The agent shouldn't have to construct/guess its own session key. Honest failure (no false success). After it (T14), the marathon's rapid follow-on injects (T15/T16) stacked on the non-completing turn → session queue stuck (known harness rapid-inject gotcha; recovered via clean-restart) | self-session resolution (UX/wiring) | low-med | 30uc-20260624 (UC-20) | `sessions_manage`/compact should resolve the CALLER's own session (a `self`/current-session path) instead of requiring a constructed key; the constructed key must use the `:peer:` form. RED: an agent-origin "compact my session" succeeds without the caller naming a key |
| **MEM-IMPORT-DUP** | `memory.portability.import` is **NOT idempotent** — re-importing an export that overlaps the existing store DUPLICATES the overlapping entries (live: 33 existing + import of a 34-entry export → **67**, `imported:34, blocked:0`). The handler blocks only CRITICAL/security entries (memory-portability-handlers.js:101), never dedups; it generates NEW ids rather than preserving/upserting the export's `id`. Violates the "no duplicate explosion" predicate (UC-22). Realistic cross-instance import (to an empty store) is unaffected; re-import / overlapping-import doubles. NOTE: recall-after-restore DOES work (canary recalled "ZEPHYR-42" after embedding completed — async timing, not a bug) | portability idempotency | med | 30uc-20260624 (UC-22) | dedup on import — upsert by the export `id` (skip/replace if present) OR content-hash dedup; report a `deduped` count. RED: importing the same export twice leaves the count unchanged the 2nd time. (left ~33 dup memories on the rig — harmless for remaining UCs) |
| **V1-NO-VISION** | The `/v1/chat/completions` OpenAI-compat endpoint **rejects multimodal/array content** — `messages.0.content: Invalid input: expected string, received array` (400). So vision (image_url content blocks, the standard OpenAI multimodal format) is NOT supported via the API, even though the platform supports vision (media.providers: vision=[anthropic,openai,google]). Combined w/ MEDIA-INPUT-SSRF (Telegram photo path blocked on loopback), vision INPUT is untestable on this rig via either path | OpenAI-compat completeness | low | 30uc-20260624 (UC-16) | accept array/multimodal `content` in the /v1 request schema → map image_url blocks to the agent's vision input (or honestly 4xx "vision not supported via /v1" naming it). RED: a /v1 request w/ image_url content is accepted (or returns a NAMED unsupported-vision error, not a generic schema 400) |
| **F-OBS-2** | `web_search`/`web_fetch` tool **results are not persisted** in the trajectory (the `tool.result` line is content-free: `{toolName,toolCallId,durationMs,success}`, no resultBytes/summary) and are NOT offloaded to `tool-results/`. So a "grounded in fetched results" predicate (UC-01 morning briefing, UC-06 deep research) can ONLY be verified by a raw **debug** daemon-log grep (`argsPreview` query + the result entities at level 20) — the load-bearing-evidence-is-DEBUG-only obs-loop trigger. The result content does reach the model, just isn't reconstructable from `explain`/trajectory | obs completeness (content-free tool.result) | low | 30uc-20260624 (UC-01) | thread a **content-free** web_search/fetch result SUMMARY onto the trajectory tool.result (result count + source domains/URLs only — never bodies) so grounding is verifiable from the trajectory without a debug grep. RED: a web_search tool.result trajectory line carries `{resultCount, domains:[...]}` |
| **F-RIG-1** | The live-test daemon launcher (`restart-m1.sh`) ran the daemon **bare** (`setsid node … &`) with **no supervisor**. Comis's config-write RPCs (config.patch/apply, heartbeat.update, gateway.restart, token create/rotate/revoke, skills_manage) call `process.kill(pid,"SIGUSR2")` on success → the SIGUSR2 handler exits **42** as a restart-hint (`setup-shutdown.ts:695`) expecting a supervisor (systemd `Restart=` / pm2) to relaunch. Bare launch → first config-mutating RPC (a `heartbeat.update`) left the daemon **dead** mid-run (30uc UC-03), and a blind retry showed "Cannot connect to daemon". NOT a product bug — the exit-42 contract is correct; the rig lacked the supervisor production provides | rig/harness (clean-slate launcher) | med (run-blocking for UC-29 + Track-M) | 30uc-20260624 (UC-03) | **DONE in-run:** added `daemon-supervisor.sh` (relaunch-on-exit-42 loop, mirrors systemd) + `restart-m1.sh` now launches under it. **Validated:** fired `heartbeat.update` → daemon exited 42 → supervisor relaunched → gateway back in ~2s. Pushed to VPS + the scripts/ kit |

## FIXED + MERGED (kept for provenance; do not re-investigate)
| id | finding | fix | merged |
|---|---|---|---|
| **OBS-SELF-DEAD** | `obs_query`/`obs_explain` agent tools call `obs.explain`+`obs.diagnostics` (`scopes:["admin"]`) → `assertNotAgentOrigin` denied the agent-origin call → "Glass Box as an agent capability" (self-observability, UC-14) was DEAD; doc-vs-code contradiction (CLAUDE.md claims agent-reachable). Same deny-by-origin/built-but-not-wired pattern as MD-02 | re-scope obs.explain+obs.diagnostics admin→rpc (read-only scrubbed digests, single-tenant); fleet/audit/billing/channels/delivery STAY admin; new arch guard `agent-obs-tools-deny-by-origin.test.ts`; 795 core-contract + 13 arch tests green; reproduced live (obs_query self-diagnose returns IncidentReport, no deny, no audit/secrets regression) | **branch `fix/telegram-media-resolver-apiroot`** commit **`2fa098de`** (30uc-20260624 UC-14; committed not pushed — USER review/merge) |
| **TELEGRAM-RESOLVER-APIROOT** | `telegram-resolver.ts:95` hardcoded the file-download URL `https://api.telegram.org/file/bot…`, IGNORING `apiRoot` — so `getFile` honored a custom apiRoot (local Bot API server / emulator) but the byte DOWNLOAD 404'd against real Telegram. Media-input dead through any custom apiRoot. Real product bug (local Bot API server support) | thread `apiRoot` into `TelegramResolverDeps` + `telegram-plugin.ts:89`; download URL = `${apiRoot ?? "https://api.telegram.org"}/file/bot…`. Test-first (RED: apiRoot→emulator-base URL); 9/9 resolver tests green; deployed+live | **branch `fix/telegram-media-resolver-apiroot`** commit **`0e6614dd`** (30uc-20260624 UC-05; committed not pushed — USER review/merge; part 2 = MEDIA-INPUT-SSRF still open) |
| **MD-02** | `memory_store` dead-by-origin — `memory.store` `scopes:["admin"]` → the agent could not persist a memory | re-scope `memory.store` admin→rpc (trust stays `learned`); arch test pins agent-memory tools agent-reachable | **#245 / #246** (`2b0e32d9`, `643aac99`) |
| **REVOKE-COUNT** | `lease.revoke {leaseId:<unknown>}` reported a phantom `{revoked:1}` (rootRunId path honestly returned 0) | `LeaseManager.revoke` returns the honest `{revoked:number}`; handler uses it instead of hardcoding 1 | **`138f2e04`** (2026-06-24, direct admin merge) |
| **REVOKE-WIRING** | `lease.revoke`/`run.kill` "Unknown RPC method" live — `leaseManager` not threaded into dispatch deps | thread `leaseManager` into `buildRpcDispatchDeps` + arch wiring-guard | #237 (`cda7ed34`) |
| **DEGRADE-GATE / BWRAP** | macOS degrade not wired to gate; Linux bwrap jail happy-path broken | degrade gating + real-bwrap orchestrate | #237 / #239 |
| **GATEWAY-ORCHCAP** | operator gateway calls to orch dual-use RPCs denied "Capability denied" (no `_capabilities` injected) | server-side orch-cap injection after the ORIGIN-02 strip | #240 |
| **KEYING-01** | per-root wall-clock budget accumulated across the interactive session → long sessions degraded | re-anchor the idle session-root wall-clock per turn | #242 (`12ce9647`) |
