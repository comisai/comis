# PI Defense Memory + Rate Limiter E2E Tests
#
# Uses port 8611 to avoid conflicts. Echo provider for deterministic responses.
# Tests: MemoryWriteValidator, InjectionRateLimiter in running daemon.

tenantId: "test"
logLevel: "debug"
dataDir: ""

agents:
  test-agent:
    name: "PIDefenseMemRateAgent"
    model: default
    thinkingLevel: "off"
    provider: default
    maxSteps: 5
    budgets:
      perExecution: 500000
      perHour: 5000000
      perDay: 50000000
    circuitBreaker:
      failureThreshold: 100
      resetTimeoutMs: 1000
      halfOpenTimeoutMs: 500
    rag:
      enabled: false

memory:
  dbPath: "test-memory-pi-defense-memrate.db"
  walMode: true

gateway:
  enabled: true
  host: "127.0.0.1"
  port: 8611
  tokens:
    - id: test-token
      secret: pi-defense-memrate-test-secret-token-xyz789
      scopes: ["rpc", "ws", "admin", "sse"]
  rateLimit:
    windowMs: 60000
    maxRequests: 10000

monitoring:
  disk:
    enabled: false
  resources:
    enabled: false
  systemd:
    enabled: false
  securityUpdates:
    enabled: false
  git:
    enabled: false
