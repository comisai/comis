---
name: sdk-fallback-skill
description: "Third-party SDK skill — no metadata, falls back to prompt-skills cluster"
type: prompt
userInvocable: true
---

# SDK Fallback Skill

Fixture: neither operator hint nor `comis.capability` metadata is provided. The capability index falls back to the reserved `prompt-skills` cluster.
