// SPDX-License-Identifier: Apache-2.0
/**
 * RED STUB — replaced by the GREEN implementation in Task 2.
 * Returns "" so the stress test fails on an assertion (not a compile error),
 * per the TDD gate "RED fails for the right reason".
 */
export function normalizeOpeningRequest(_signature: string): string {
  return "";
}
