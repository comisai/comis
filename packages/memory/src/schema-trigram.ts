// SPDX-License-Identifier: Apache-2.0
/**
 * STUB (Task 1, RED). Replaced by the real DDL in Task 2.
 *
 * @module
 */

import type Database from "better-sqlite3";

/** STUB — empty body so the RED suite fails behaviorally (missing twins/triggers),
 *  not on a missing import. Implemented in Task 2. */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function ensureTrigramTwins(db: Database.Database): void {
  // intentionally empty (RED)
}
