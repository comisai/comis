/**
 * Scheduler configuration schema -- re-exported from @comis/core.
 *
 * The canonical schema lives in @comis/core/config/schema-scheduler.ts
 * and is used in AppConfigSchema. This re-export provides standalone access
 * for the scheduler package without duplicating the schema definition.
 */
export { SchedulerConfigSchema } from "@comis/core";
export type { SchedulerConfig } from "@comis/core";
