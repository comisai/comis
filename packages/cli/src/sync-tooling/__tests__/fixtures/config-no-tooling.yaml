# Fixture: config without a `tooling:` block.
# Consumed by generate-skeleton unit tests and the integration test.

integrations:
  mcp:
    servers:
      - name: yfinance
        transport: stdio
        command: npx
        args:
          - -y
          - yfinance-mcp-ts

agents:
  default:
    skills:
      discoveryPaths:
        - ./skills
